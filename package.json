{
  "name": "vanilla-js-bundler",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "gh-deploy": "git push origin :gh-pages && git subtree push --prefix dist origin gh-pages",
    "gh-deploy-init": "git push origin && git subtree push --prefix dist origin gh-pages",
    "dev": "vite",
    "build": "vite build",
    "preview-build": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.2.0"
  },
  "engines": {
    "node": "=20.12.1",
    "npm": "=10.5.0"
  },
  "dependencies": {
    "axios": "^1.6.8"
  }
}
